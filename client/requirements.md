## Packages
framer-motion | Complex animations for flashcard flips and page transitions
canvas-confetti | Celebration effects for correct answers/high scores
@types/canvas-confetti | Types for confetti
clsx | Conditional class names
tailwind-merge | Merging tailwind classes

## Notes
Backend provides /api/terms for all vocabulary data.
Frontend needs to handle client-side filtering (Course 1 vs 2, Units) based on the fetched data.
State for game progress (score, current card) will be local React state.
